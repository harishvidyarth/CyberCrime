# Pinned to known-working versions (tested on Python 3.11) so that
# `pip install -r requirements.txt` is reproducible and never pulls a breaking new
# release. Bump versions deliberately, then re-run tests/smoke_test.py.
# NOTE: requires Python 3.10+ (the CVE-fixed releases of requests/urllib3/pillow
# dropped 3.9, which is EOL since Oct 2025).
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
pandas==3.0.3
Flask-Login==0.6.3
Flask-Migrate==4.1.0
PyMySQL==1.2.0
cryptography==48.0.1
openpyxl==3.1.5
requests==2.33.0          # >=2.33 fixes GHSA-gc5v-m9x4-r6x2
python-dotenv==1.2.2      # >=1.2.2 fixes GHSA-mf9w-mj56-hr94
urllib3==2.7.0            # transitive (requests); pinned for PYSEC-2026-141/142
pillow==12.2.0            # transitive (xhtml2pdf/reportlab); pinned for 6 CVEs
Flask-WTF==1.2.2
Flask-Limiter==3.11.0
Flask-Compress==1.18      # gzip responses (Feature D20)
pyotp==2.9.0              # TOTP two-factor auth (Feature B9)
qrcode==8.0               # QR code generation for 2FA setup page (Pillow already required)
pytest-cov==7.0.0         # coverage reporting in CI
sentry-sdk[flask]==2.47.0 # optional error/performance telemetry when SENTRY_DSN is set
resend==2.21.0            # password reset email delivery

# Two PDF tools, kept on purpose (Phase 6, item 24): xhtml2pdf renders the HTML letter
# templates; reportlab draws the data-visual reports (fund-trail, analytics). Migrating
# letters to reportlab would be a risky rewrite of core output for little real gain.
xhtml2pdf==0.2.17
reportlab==4.5.1
python-docx==1.2.0
